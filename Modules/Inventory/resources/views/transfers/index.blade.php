@extends('layouts.app')
@section('title', 'تحويلات الفروع')
@section('content')
<div class="space-y-7"><div class="flex items-end justify-between gap-4 border-b border-slate-200 pb-6"><div><a href="{{ route('inventory.balances.index') }}" class="text-sm font-bold text-indigo-600">أرصدة المخزون</a><h1 class="mt-3 text-3xl font-extrabold">تحويلات الفروع</h1><p class="mt-2 text-sm text-slate-600">مستندات نقل المخزون بين الفروع.</p></div><a href="{{ route('inventory.transfers.create') }}" class="rounded-xl bg-indigo-600 px-5 py-3 text-sm font-bold text-white">تحويل جديد</a></div>@if(session('status'))<div class="rounded-xl bg-emerald-50 p-4 text-emerald-800">{{ session('status') }}</div>@endif<section class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">@forelse($transfers as $transfer)<a href="{{ route('inventory.transfers.show', $transfer) }}" class="flex items-center justify-between gap-4 border-b border-slate-100 p-5 hover:bg-slate-50"><div><p class="font-bold">{{ $transfer->sourceBranch->name }} ← {{ $transfer->destinationBranch->name }}</p><p class="mt-1 text-sm text-slate-600">{{ $transfer->items_count }} أصناف · {{ $transfer->creator->name }} · {{ $transfer->created_at->format('Y/m/d H:i') }}</p></div><span class="rounded-full bg-slate-100 px-3 py-1 text-sm font-bold">{{ $transfer->status->value }}</span></a>@empty<div class="px-6 py-16 text-center text-slate-600">لا توجد تحويلات فروع مسجلة بعد.</div>@endforelse</section></div>
@endsection
