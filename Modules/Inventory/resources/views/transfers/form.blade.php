@extends('layouts.app')
@section('title', 'تحويل مخزون جديد')
@section('content')
<div class="mx-auto max-w-4xl space-y-7"><div class="border-b border-slate-200 pb-6"><a href="{{ route('inventory.transfers.index') }}" class="text-sm font-bold text-indigo-600">تحويلات الفروع</a><h1 class="mt-3 text-3xl font-extrabold">تحويل مخزون جديد</h1></div>@if($errors->any())<div class="rounded-xl bg-red-50 p-4 text-red-800" role="alert">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('inventory.transfers.store') }}" class="space-y-5 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">@csrf<input type="hidden" name="idempotency_key" value="{{ old('idempotency_key', (string) \Illuminate\Support\Str::uuid()) }}"><div class="grid gap-5 sm:grid-cols-2"><label class="font-bold">من فرع<select name="source_branch_id" class="mt-2 w-full rounded-xl border-slate-300">@foreach($branches as $branch)<option value="{{ $branch->id }}">{{ $branch->name }}</option>@endforeach</select></label><label class="font-bold">إلى فرع<select name="destination_branch_id" class="mt-2 w-full rounded-xl border-slate-300">@foreach($branches as $branch)<option value="{{ $branch->id }}">{{ $branch->name }}</option>@endforeach</select></label></div><label class="block font-bold">السبب<textarea name="reason" class="mt-2 w-full rounded-xl border-slate-300" required>{{ old('reason') }}</textarea></label><div class="grid gap-3 sm:grid-cols-[1fr_9rem]"><select name="items[0][product_id]" class="rounded-xl border-slate-300">@foreach($products as $product)<option value="{{ $product->id }}">{{ $product->name }}</option>@endforeach</select><input name="items[0][quantity]" type="number" min="1" value="{{ old('items.0.quantity', 1) }}" class="rounded-xl border-slate-300"></div><button class="rounded-xl bg-indigo-600 px-5 py-3 font-bold text-white">تسجيل التحويل</button></form></div>
@endsection
