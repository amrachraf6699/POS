@extends('tracker::layouts.master')

@section('title', $issue['label'].' · POS MVP Tracker')
@section('content')
<main class="min-h-screen"><div class="mx-auto max-w-4xl px-5 py-8 sm:px-8"><div class="mb-8 flex items-center justify-between gap-4"><a href="{{ route('tracker.problems') }}" class="text-sm text-zinc-500 hover:text-white">← All problems</a><a href="{{ $issue['task_id'] ? route('tracker.tasks.show', [$issue['phase_id'], $issue['task_id']]) : route('tracker.phases.show', $issue['phase_id']) }}" class="text-sm text-indigo-300 hover:text-indigo-200">View source →</a></div><div class="rounded-xl border {{ $issue['type'] === 'problem' ? 'border-amber-500/30' : 'border-rose-500/30' }} bg-[#111113] p-6 sm:p-8"><div class="flex flex-wrap items-center gap-3"><span class="rounded-md {{ $issue['type'] === 'problem' ? 'bg-amber-500/15 text-amber-300' : 'bg-rose-500/15 text-rose-300' }} px-2 py-1 text-xs font-medium">{{ $issue['label'] }}</span><span class="font-mono text-xs text-zinc-600">{{ $issue['id'] }}</span></div><h1 class="mt-5 text-2xl font-semibold leading-tight text-white">{{ $issue['message'] }}</h1><dl class="mt-6 grid gap-4 border-y border-zinc-800 py-5 text-sm sm:grid-cols-2"><div><dt class="text-xs text-zinc-600">Phase</dt><dd class="mt-1 text-zinc-300">{{ $issue['phase_title'] }}</dd></div><div><dt class="text-xs text-zinc-600">Task</dt><dd class="mt-1 text-zinc-300">{{ $issue['task_title'] ?: 'Phase-level issue' }}</dd></div><div><dt class="text-xs text-zinc-600">Current status</dt><dd class="mt-1 text-zinc-300">{{ str_replace('_', ' ', $issue['status']) }}</dd></div><div><dt class="text-xs text-zinc-600">Latest commit</dt><dd class="mt-1 font-mono text-zinc-300">{{ $issue['latest_commit'] ?: 'Not available' }}</dd></div></dl><h2 class="mt-7 text-sm font-medium text-white">Record a resolution</h2><p class="mt-1 text-sm text-zinc-500">This removes the open issue from tracker state and appends a timestamped resolution to its source record.</p>@if (session('status'))<div class="mt-4 rounded-lg border border-emerald-500/30 bg-emerald-500/10 px-4 py-3 text-sm text-emerald-300">{{ session('status') }}</div>@endif<form method="post" action="{{ route('tracker.problems.resolve', $issue['id']) }}" class="mt-4">@csrf<textarea name="resolution" required maxlength="2000" rows="5" placeholder="What changed, and what evidence proves the issue is resolved?" class="w-full rounded-lg border border-zinc-700 bg-zinc-900 px-3 py-3 text-sm text-white outline-none placeholder:text-zinc-600 focus:border-indigo-500"></textarea>@error('resolution')<p class="mt-2 text-xs text-rose-300">{{ $message }}</p>@enderror<button class="mt-3 rounded-lg bg-indigo-500 px-4 py-2.5 text-sm font-medium text-white hover:bg-indigo-400">Mark resolved</button></form></div></div></main>
@endsection
